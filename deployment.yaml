apiVersion: apps/v1
kind: Deployment
metadata:
  name: brain-tasks
spec:
  replicas: 2
  selector:
    matchLabels:
      app: brain-tasks
  template:
    metadata:
      labels:
        app: brain-tasks
    spec:
      containers:
      - name: brain-tasks
        image: 867344456453.dkr.ecr.ap-south-1.amazonaws.com/brain-tasks:latest
        ports:
        - containerPort: 3000
